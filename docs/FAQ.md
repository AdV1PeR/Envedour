# FAQ - Часто задаваемые вопросы

Ответы на часто задаваемые вопросы об Envedour Bot.

## Общие вопросы

### Можно ли использовать на x86_64?

**Да**, бот работает на любой архитектуре, но оптимизации для ARM64 будут недоступны. Соберите без флага `--arm-optimized`:

```bash
make build
```

### Нужен ли локальный Bot API?

**Не обязательно**, но **рекомендуется** для:
- Снижения нагрузки на серверы Telegram
- Увеличения пропускной способности
- Снижения задержек

Бот может работать и с официальным Bot API, просто не устанавливайте `LOCAL_API_URL`.

### Сколько пользователей может обслуживать бот?

Зависит от железа:
- **OrangePi Zero3 (4GB)**: 50-100 одновременных пользователей
- **Raspberry Pi 4 (4GB+)**: 50-100 одновременных пользователей
- **Более мощное железо**: 150+ пользователей

### Сохраняются ли скачанные файлы?

**Нет**, файлы автоматически удаляются после отправки пользователю. Это сделано для:
- Экономии места на диске
- Приватности (файлы не хранятся)
- Соблюдения лицензий

### Можно ли скачивать плейлисты?

В текущей версии поддерживается только **одно видео за раз**. Плейлисты можно добавить как отдельную функцию в будущем.

## Технические вопросы

### Почему TikTok не работает?

TikTok требует:
1. **Свежие cookies** - экспортируйте из браузера регулярно
2. **curl-cffi** - для TLS fingerprinting bypass

**Решение**:
```bash
sudo pip3 install curl-cffi
sudo pip3 install --upgrade "yt-dlp[default,curl-cffi]"
```

### Как обновить yt-dlp?

```bash
sudo pip3 install --upgrade "yt-dlp[default,curl-cffi]"
```

### Можно ли изменить порт Bot API?

**Да**, измените `TELEGRAM_HTTP_PORT` в `.env` и обновите `LOCAL_API_URL` соответственно:

```bash
TELEGRAM_HTTP_PORT=9090
LOCAL_API_URL=http://localhost:9090
```

### Как добавить больше доноров?

Добавьте chat ID в `DONOR_CHAT_IDS` через запятую:

```bash
DONOR_CHAT_IDS=123456789,987654321,111222333
```

**Как узнать Chat ID**: Отправьте сообщение боту @userinfobot

### Почему бот использует так много памяти?

Бот обрабатывает большие видеофайлы. Для снижения использования памяти:

1. Уменьшите `MAX_FILE_SIZE_MB`
2. Уменьшите `WORKER_COUNT`
3. Увеличьте `MIN_FREE_MEM_MB`

### Можно ли использовать без Redis?

**Нет**, Redis необходим для:
- Очереди задач
- Хранения настроек пользователей
- Временного хранения URL

## Вопросы по установке

### Можно ли установить без sudo?

**Нет**, установка требует root прав для:
- Создания пользователя `botuser`
- Установки systemd сервисов
- Настройки системы (sysctl, limits, fstab)

### Нужен ли Docker?

**Нет**, бот работает как нативный systemd сервис. Docker был удален из проекта.

### Как обновить бота?

```bash
# 1. Остановите сервис
sudo systemctl stop envedour-down-bot

# 2. Обновите код
git pull

# 3. Пересоберите
make build-arm

# 4. Переустановите
sudo ./deploy/install.sh

# 5. Запустите
sudo systemctl start envedour-down-bot
```

### Можно ли запустить несколько экземпляров?

**Да**, но нужно:
- Разные порты для Bot API
- Разные директории
- Разные .env файлы

Не рекомендуется без необходимости.

## Вопросы по использованию

### Почему Instagram/TikTok скачиваются автоматически?

Это сделано для удобства - эти платформы обычно имеют только одно качество, поэтому запрос не нужен.

### Можно ли изменить качество по умолчанию?

**Да**, настройки сохраняются для каждого пользователя. Выберите качество через кнопки, и оно будет использоваться для следующих скачиваний.

### Почему MP3 файлы имеют правильные названия?

Бот автоматически извлекает название видео из метаданных и использует его для имени MP3 файла.

### Можно ли скачивать только аудио?

**Да**, выберите "🎵 Аудио" в меню качества или "🎵 Аудио (MP3)" в меню типа медиа.

### Почему некоторые видео не скачиваются?

Возможные причины:
1. Видео недоступно или удалено
2. Ограничения платформы (требуются cookies)
3. Файл слишком большой
4. Недостаточно памяти

## Вопросы по производительности

### Как увеличить скорость скачивания?

1. Увеличьте `WORKER_COUNT` (но не больше ядер CPU)
2. Используйте локальный Bot API
3. Оптимизируйте сеть (TCP tuning)
4. Используйте качественный интернет

### Как снизить использование памяти?

1. Уменьшите `MAX_FILE_SIZE_MB`
2. Уменьшите `WORKER_COUNT`
3. Уменьшите размер tmpfs
4. Увеличьте `MIN_FREE_MEM_MB`

### Почему бот медленно обрабатывает задачи?

Возможные причины:
1. Высокая температура CPU (throttling)
2. Недостаточно памяти
3. Много задач в очереди
4. Медленный интернет

**Решения**: См. [TROUBLESHOOTING.md](TROUBLESHOOTING.md)

## Вопросы по безопасности

### Безопасно ли хранить BOT_TOKEN в .env?

**Да**, файл `.env` имеет права доступа только для владельца. Убедитесь, что права установлены правильно:

```bash
sudo chmod 600 /opt/envedour-bot/.env
sudo chown botuser:botuser /opt/envedour-bot/.env
```

### Сохраняются ли пользовательские данные?

**Нет**, бот не сохраняет:
- Chat ID (кроме доноров в конфигурации)
- URL (только временно для callback queries)
- Скачанные файлы (удаляются после отправки)

### Можно ли использовать без cookies?

**Зависит от платформы**:
- **YouTube**: Да, работает без cookies
- **TikTok**: Нет, cookies обязательны
- **Instagram**: Нет, cookies обязательны

## Вопросы по платформам

### Какие платформы поддерживаются?

**Полная поддержка**:
- YouTube
- TikTok (требуются cookies)
- Instagram (требуются cookies)

**Частичная поддержка**:
- VK (через yt-dlp)
- Другие платформы (через yt-dlp)

### Почему TikTok требует cookies?

TikTok использует защиту от ботов. Cookies и curl-cffi необходимы для обхода этой защиты.

### Можно ли добавить поддержку новой платформы?

**Да**, см. раздел "Разработка" в [ARCHITECTURE.md](ARCHITECTURE.md).

## Вопросы по ошибкам

### Что делать при ошибке "insufficient memory"?

1. Проверьте использование памяти: `free -h`
2. Уменьшите `MAX_FILE_SIZE_MB`
3. Уменьшите `WORKER_COUNT`
4. Увеличьте `MIN_FREE_MEM_MB`

### Что делать при ошибке "403 Forbidden"?

1. Обновите cookies файлы
2. Убедитесь, что установлен curl-cffi
3. Обновите yt-dlp

### Что делать при ошибке "file too large"?

1. Увеличьте `MAX_FILE_SIZE_MB`
2. Или выберите меньшее качество

### Что делать если бот не отвечает?

1. Проверьте статус: `sudo systemctl status envedour-down-bot`
2. Проверьте логи: `sudo journalctl -u envedour-down-bot -n 100`
3. Проверьте Bot API: `sudo systemctl status telegram-bot-api`

## Вопросы по конфигурации

### Где находится .env файл?

После установки:
- `/opt/envedour-bot/.env` - для бота
- `/opt/telegram-bot-api/.env` - для Bot API

### Как изменить настройки после установки?

```bash
# Отредактируйте .env
sudo nano /opt/envedour-bot/.env

# Перезапустите сервис
sudo systemctl restart envedour-down-bot
```

### Можно ли использовать переменные окружения вместо .env?

**Да**, systemd сервисы читают переменные из:
1. Файла `.env` (EnvironmentFile)
2. Переменных окружения в systemd (Environment)

---

**Не нашли ответ?** См. [TROUBLESHOOTING.md](TROUBLESHOOTING.md) или создайте issue
