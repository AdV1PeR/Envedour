# Cron jobs for Telegram Bot maintenance

# Daily backup at 2 AM
0 2 * * * /opt/envedour-bot/scripts/backup.sh >> /var/log/envedour-bot-backup.log 2>&1

# Health check every 5 minutes
*/5 * * * * /opt/envedour-bot/scripts/health-check.sh >> /var/log/envedour-bot-health.log 2>&1

# Cleanup old files from tmpfs daily at 3 AM
0 3 * * * find /dev/shm/videos -type f -mtime +1 -delete
